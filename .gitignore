# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — ignore everything .env* EXCEPT the committed template
.env*
!.env.example

# vercel
.vercel

# Local Netlify folder
.netlify
# Netlify edge-functions Deno lock (regenerated by `netlify dev`/`build`)
deno.lock
