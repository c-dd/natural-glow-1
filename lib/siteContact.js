// ---------------------------------------------------------------------------
// Public contact details for the storefront (contact page, etc.).
//
// ⚠️  CLIENT MUST REPLACE — placeholder values.
// `email` is a placeholder address on a placeholder domain; it is not a
// monitored inbox. Point it at the real business inbox (and register the
// domain) before launch. Single source of truth so the client swaps it once.
// ---------------------------------------------------------------------------
export const SITE_CONTACT = {
  // General enquiries + lot / COA questions (one inbox placeholder).
  email: 'contact@naturalglow.bio',
  // Human-readable expected response window, shown in the contact copy.
  responseTime: 'usually within two business days',
};
